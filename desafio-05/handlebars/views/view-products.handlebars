<div class="container"><a href="/"><button class="btn btn-primary">Volver </button></a>
    <table class="table table-striped table-hover mt-5">
        <thead>
            <tr>
                <th class="text-center" scope="col">ID </th>
                <th class="text-center" scope="col">Descripcion </th>
                <th class="text-center" scope="col">Precio </th>
                <th class="text-center" scope="col">Icono </th>
            </tr>
        </thead>
        <tbody>
            {{#if productos}}
                {{#each productos}}
                    <tr>
                        <td class="text-end">{{this.id}}</td>
                        <td class="text-start ps-4">{{this.title}}</td>
                        <td class="text-end ps-4">$ {{this.price}}</td>
                        <td class="text-center"><img src={{this.thumbnail}} alt={{this.title}} style="width: 50px; height: 50px"></td>
                    </tr>
                {{/each}}
            {{/if}}
        </tbody>
    </table>
</div>